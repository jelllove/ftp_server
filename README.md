# ftp_server
A very simple FTP server, implemented basic service for the FTP server.


# FTP服务器
C++代码写的一个迷你的FTP服务端软件，可以完成基本的用户权限控制，目录列表，文件上传/下截等功能。
支持多个用户，同时支持多用户同时操作。
写的时候是在Windows上写的，所以默认是在Windows上运行，但是只要把socket这块做一点改动就可以跑在Linux上了。
如果这个代码对你有帮忙的话，请随意~
